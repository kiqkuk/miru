#!/bin/bash

SCRIPT_PATH=$(realpath "$0")
MIRU_DIR=$(dirname "$(dirname "$SCRIPT_PATH")")
CONFIG_DIR="$MIRU_DIR/config"
BIN_SRC="$MIRU_DIR/bin"
BIN_TARGET="$HOME/.local/bin"

[[ ! -d "$CONFIG_DIR" ]] && { echo "ERROR: config folder not found"; exit 1; }

echo "Checking for broken links..."
fd -t l -H . "$HOME/.config" "$BIN_TARGET" 2>/dev/null | while read -r link; do
  [[ ! -e "$link" ]] && rm -v "$link"
done

if [[ -d "$CONFIG_DIR" ]]; then
  echo "Checking user config..."
  fd -t f -H . "$CONFIG_DIR" | while read -r DOT_FILE; do
    REL_FILE="${DOT_FILE#$CONFIG_DIR/}"
    TARGET_FILE="$HOME/.config/$REL_FILE"
    [[ -e "$TARGET_FILE" || -L "$TARGET_FILE" ]] && continue
    mkdir -p "$(dirname "$TARGET_FILE")"
    ln -sfv "$DOT_FILE" "$TARGET_FILE"
  done
fi

if [[ -d "$BIN_SRC" ]]; then
  echo "Checking scripts on $BIN_TARGET..."
  mkdir -p "$BIN_TARGET"
  fd -t f . "$BIN_SRC" | while read -r SCRIPT; do
    NAME=$(basename "$SCRIPT")
    TARGET_BIN="$BIN_TARGET/$NAME"
    [[ -e "$TARGET_BIN" || -L "$TARGET_BIN" ]] && continue
    chmod +x "$SCRIPT"
    ln -sfv "$SCRIPT" "$TARGET_BIN"
  done
fi

if [[ -d "/etc/zsh" ]]; then
  ZSHENV="/etc/zsh/zshenv"
  Z_CONF="export ZDOTDIR=\"\$HOME/.config/zsh\""
  echo "Checking $ZSHENV"
  if ! rg -q "^export ZDOTDIR" "$ZSHENV" 2>/dev/null; then
    echo "Adding ZDOTDIR to $ZSHENV..."
    echo "$Z_CONF" | sudo tee -a "$ZSHENV" > /dev/null
  fi
fi

echo "done"
