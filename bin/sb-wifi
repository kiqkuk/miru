#!/bin/bash
# miru: sb-wifi | kiqkuk

ssid=$(iwctl station wlan0 show | sed -n 's/.*Connected network[[:space:]]*//p' | sed 's/\x1b\[[0-9;]*m//g' | xargs)

if [ -z "$ssid" ]; then
    echo "󰤮 Disconnected"
else
    echo "󰖩 $ssid"
fi
